rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Allow all reads and writes to all documents for emulator testing ONLY
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 