specVersion: "v1"
serviceId: "splittingsucks"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "splittingsucks-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
