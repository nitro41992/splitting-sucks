rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // User profile access
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // Receipts subcollection
      match /receipts/{receiptId} {
        // Owner can do anything
        allow read, create, update, delete: if request.auth != null && request.auth.uid == userId;
        // NOTE: The permissive rule for "test-user-id" has been removed for production.
      }
    }
    
    // Existing top-level receipts collection (if used in production)
    match /receipts/{receiptId} {
      allow read: if request.auth != null && resource.data.ownerId == request.auth.uid;
      allow create: if request.auth != null && request.resource.data.ownerId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.ownerId == request.auth.uid; 
    }
    
    // Optional: A general deny-all rule if not all paths are covered above,
    // ensuring no unintended access.
    // match /{document=**} {
    //   allow read, write: if false;
    // }
  }
}